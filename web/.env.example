# API Configuration
# For local dev with 'npm run dev': set to backend URL (e.g., http://localhost:3000)
# For Docker builds: leave empty to use relative URLs (nginx proxy handles routing)
VITE_API_BASE_URL=http://localhost:3000
